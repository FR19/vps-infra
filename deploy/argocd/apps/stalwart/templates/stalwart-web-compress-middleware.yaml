{{- if .Values.hostname }}
# Compress webadmin responses (WASM ~6MB compresses to ~1–2MB with gzip/brotli) to speed up load.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: stalwart-web-compress
  namespace: {{ .Release.Namespace | quote }}
spec:
  compress:
    minResponseBodyBytes: 1024
{{- end }}
