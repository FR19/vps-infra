apiVersion: v1
kind: Service
metadata:
  name: headplane
  namespace: headplane
  labels:
    app.kubernetes.io/name: headplane
spec:
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app.kubernetes.io/name: headplane
