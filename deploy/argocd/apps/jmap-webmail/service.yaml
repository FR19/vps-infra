apiVersion: v1
kind: Service
metadata:
  name: jmap-webmail
  namespace: webmail
  labels:
    app.kubernetes.io/name: jmap-webmail
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: jmap-webmail
