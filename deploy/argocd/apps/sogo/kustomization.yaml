apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sogo
resources:
  - namespace.yaml
  # salvoxia: plist config; sonroyaalmerol image fails with "cannot find adaptor bundle: 'ldap'" (image may be built without LDAP).
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - ingressroute.yaml
