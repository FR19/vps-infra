{{- if and .Values.mailu .Values.mailu.authentik .Values.mailu.authentik.enabled }}
# ForwardAuth middleware for Authentik. Used by the Mailu IngressRoute when authentik.enabled.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: mailu-authentik-forward-auth
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  forwardAuth:
    address: {{ .Values.mailu.authentik.forwardAuthUrl | quote }}
    trustForwardHeader: true
    # Headers sent to Authentik. Must include Cookie so the session set at callback is sent on subsequent requests.
    # Without Cookie, Authentik always returns 302 to login → "too many redirects".
    authRequestHeaders:
      - Cookie
      - Host
      - X-Forwarded-Host
      - X-Forwarded-Proto
      - X-Forwarded-Uri
      - X-Forwarded-Method
      - X-Forwarded-For
    authResponseHeaders:
      - X-authentik-username
      - X-authentik-groups
      - X-authentik-entitlements
      - X-authentik-email
      - X-authentik-name
      - X-authentik-uid
      - X-authentik-jwt
      - X-authentik-meta-jwks
      - X-authentik-meta-outpost
      - X-authentik-meta-provider
      - X-authentik-meta-app
      - X-authentik-meta-version
{{- end }}
